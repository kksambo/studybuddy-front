import React from "react";
import { Typography } from "@mui/material";

const Profile = () => (
  <div>
    <Typography variant="h4" gutterBottom>
      My Profile
    </Typography>
    <Typography>Profile details and settings will appear here.</Typography>
  </div>
);

export default Profile;
