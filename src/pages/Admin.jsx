import React from "react";
import { Typography } from "@mui/material";

const Admin = () => (
  <div>
    <Typography variant="h4" gutterBottom>
      Admin Panel
    </Typography>
    <Typography>Admin actions will appear here.</Typography>
  </div>
);

export default Admin;
